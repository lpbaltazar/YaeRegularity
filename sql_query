SELECT USERID, SESSIONID, CONTENT_TYPE, MAX(PRIMARY_FINGERPRINT), SESSION_STARTDT_YEAR, SESSION_STARTDT_MONTH, SESSION_STARTDT_DAY, HOUR(SESSION_STARTDT)
FROM events_data2
GROUP BY USERID, SESSIONID, CONTENT_TYPE